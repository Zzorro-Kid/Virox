import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF1F234D);

  // Accent colors
  static const accent = Color(0xFFBC2A34);

  // Background colors
  static const background = Color(0xFFFFFFFF);
  static const backgroundDark = Color(0xFF1F234D);
  static const surfaceBackground = Color(0xFFF7F7F7);
  static const keyboardBackground = Color(0xFFE9EAEE);

  // Text colors
  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFF475362);
  static const textOnDark = Color(0xFFFFFFFF);
  static const textKeyboard = Color(0xFF3A3B3D);
  static const textDark = Color(0xFF12181E);

  // Border colors
  static const border = Color(0xFFECECEC);
  static const borderKeyboard = Color(0x1A000000);
  static const borderSearchBar = Color(0xB3000000);

  // Key colors
  static const keyDefault = Color(0xFFFFFFFF);
  static const keySpecial = Color(0xFFCBCED5);
  static const keyShadow = Color(0xFF898A8D);

  // Icon colors
  static const iconPrimary = Color(0xFF1F234D);
  static const emptyPlaceholderIcon = Color(0xFFD9D9D9);
}
